{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the popover + dashboard windows",
  "windows": ["main", "dashboard"],
  "permissions": [
    "core:default",
    "notification:default"
  ]
}
